{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist",
        "nodeVersion": "18",
        "installCommand": "npm install --force"
      }
    }
  ]
}
